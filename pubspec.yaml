name: news_app_internship
description: Creating a news app as an assignment of internship

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

  http: ^0.13.4
  url_launcher: ^6.0.12
  flutter_launcher_icons: "^0.9.2"
  flutter_spinkit: ^5.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo.webp"

flutter:
  uses-material-design: true

  assets:
    - assets/
